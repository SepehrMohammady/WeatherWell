diff --git a/node_modules/react-native-android-widget/android/src/main/java/com/reactnativeandroidwidget/RNWidgetUtil.java b/node_modules/react-native-android-widget/android/src/main/java/com/reactnativeandroidwidget/RNWidgetUtil.java
index 2a34305..f586b93 100644
--- a/node_modules/react-native-android-widget/android/src/main/java/com/reactnativeandroidwidget/RNWidgetUtil.java
+++ b/node_modules/react-native-android-widget/android/src/main/java/com/reactnativeandroidwidget/RNWidgetUtil.java
@@ -17,21 +17,28 @@ import com.facebook.react.bridge.WritableMap;
 import java.util.List;
 
 public class RNWidgetUtil {
+    // Use MAX dimensions in both orientations to ensure bitmap fills widget container
+    // Some launchers (MIUI/HyperOS) report MIN dimensions smaller than actual container
     public static int getWidgetWidth(Context context, int widgetId) {
         boolean isPortrait = context.getResources().getConfiguration().orientation == ORIENTATION_PORTRAIT;
+        int minWidth = getWidgetSizeInDp(context, widgetId, AppWidgetManager.OPTION_APPWIDGET_MIN_WIDTH);
+        int maxWidth = getWidgetSizeInDp(context, widgetId, AppWidgetManager.OPTION_APPWIDGET_MAX_WIDTH);
         if (isPortrait) {
-            return getWidgetSizeInDp(context, widgetId, AppWidgetManager.OPTION_APPWIDGET_MIN_WIDTH);
+            // Use the larger of MIN_WIDTH and MAX_WIDTH to avoid gaps
+            return Math.max(minWidth, maxWidth);
         } else {
-            return getWidgetSizeInDp(context, widgetId, AppWidgetManager.OPTION_APPWIDGET_MAX_WIDTH);
+            return Math.max(minWidth, maxWidth);
         }
     }
 
     public static int getWidgetHeight(Context context, int widgetId) {
         boolean isPortrait = context.getResources().getConfiguration().orientation == ORIENTATION_PORTRAIT;
+        int minHeight = getWidgetSizeInDp(context, widgetId, AppWidgetManager.OPTION_APPWIDGET_MIN_HEIGHT);
+        int maxHeight = getWidgetSizeInDp(context, widgetId, AppWidgetManager.OPTION_APPWIDGET_MAX_HEIGHT);
         if (isPortrait) {
-            return getWidgetSizeInDp(context, widgetId, AppWidgetManager.OPTION_APPWIDGET_MAX_HEIGHT);
+            return Math.max(minHeight, maxHeight);
         } else {
-            return getWidgetSizeInDp(context, widgetId, AppWidgetManager.OPTION_APPWIDGET_MIN_HEIGHT);
+            return Math.max(minHeight, maxHeight);
         }
     }
 
