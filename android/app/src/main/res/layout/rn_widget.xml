<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@android:id/background"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/rn_widget_image_light"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/transparent"
        android:scaleType="fitXY"
        android:visibility="visible" />

    <ImageView
        android:id="@+id/rn_widget_image_dark"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/transparent"
        android:scaleType="fitXY"
        android:visibility="gone" />

    <FrameLayout
        android:id="@+id/rn_widget_clickable_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <FrameLayout
        android:id="@+id/rn_widget_collection_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Native refresh control (FeedWell-style): the button is the only refresh
         icon; WeatherWidget.java swaps it for the spinner via partial updates
         while the JS headless refresh runs, then WidgetPinModule restores it. -->
    <FrameLayout
        android:id="@+id/widget_refresh_overlay"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_gravity="top|end"
        android:layout_marginTop="9dp"
        android:layout_marginEnd="9dp">

        <ImageButton
            android:id="@+id/widget_refresh_button"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@null"
            android:src="@drawable/ic_widget_refresh"
            android:scaleType="centerInside"
            android:padding="5dp"
            android:contentDescription="Refresh weather" />

        <ProgressBar
            android:id="@+id/widget_refresh_progress"
            style="?android:attr/progressBarStyleSmall"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="4dp"
            android:indeterminate="true"
            android:indeterminateTint="#B6BCBE"
            android:background="@drawable/widget_refresh_badge"
            android:visibility="gone" />
    </FrameLayout>
</FrameLayout>
